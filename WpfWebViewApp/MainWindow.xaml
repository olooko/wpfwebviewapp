<Window x:Class="WpfWebViewApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="450" Width="800"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Window.Resources>

        <Color x:Key="WindowFrameColor">#DCDCDC</Color>
        
        <Style x:Key="ControlBarButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border CornerRadius="4" BorderThickness="1">
        <Border.Background>
            <SolidColorBrush Color="{StaticResource WindowFrameColor}" />
        </Border.Background>
        <Border.BorderBrush>
            <SolidColorBrush Color="{StaticResource WindowFrameColor}" />
        </Border.BorderBrush>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition />
                <RowDefinition Height="4" />
            </Grid.RowDefinitions>
            <Border CornerRadius="4,4,0,0" BorderThickness="0" Background="White" MouseDown="TopBorder_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock x:Name="MainDesc" />
                        <Button Click="Button_Click">Send</Button>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <RadioButton GroupName="WebView" Name="Edge" Checked="RadioButton_Checked" IsChecked="True">Edge</RadioButton>
                        <RadioButton GroupName="WebView" Name="Chrome" Checked="RadioButton_Checked">Chrome</RadioButton>
                        <Button Style="{StaticResource ControlBarButton}" Click="MinimizeButton_Click">
                            <Image Source="/Images/icon_minimize.png" />
                        </Button>
                        <Button Style="{StaticResource ControlBarButton}" Click="MaximizeButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image x:Name="MaximizeImage" Source="/Images/icon_maximize.png" />
                                <Image x:Name="NormalImage" Source="/Images/icon_normal.png" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource ControlBarButton}" Click="CloseButton_Click">
                            <Image Source="/Images/icon_close.png" />
                        </Button>
                    </StackPanel>
                    <Rectangle Height="1" VerticalAlignment="Bottom">
                        <Rectangle.Fill>
                            <SolidColorBrush Color="{StaticResource WindowFrameColor}" />
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>
            </Border>
            <wv2:WebView2 x:Name="WV2" Grid.Row="1" Source="https://erasebg.cafe24.com" Visibility="Visible" WebMessageReceived="WV2_WebMessageReceived" />
            <cef:ChromiumWebBrowser x:Name="CEF" Grid.Row="1" Address="https://erasebg.cafe24.com" Visibility="Collapsed" JavascriptMessageReceived="CEF_JavascriptMessageReceived" />
        </Grid>
    </Border>
</Window>
